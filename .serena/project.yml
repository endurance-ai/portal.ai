project_name: "MOODFIT (fashion-ai)"

languages:
  - typescript

encoding: "utf-8"

ignore_all_files_in_gitignore: true

ignored_paths:
  - "**/node_modules/**"
  - "**/dist/**"
  - "**/.next/**"
  - "**/pnpm-lock.yaml"

read_only: false

excluded_tools: []
included_optional_tools: []
fixed_tools: []

base_modes:
default_modes:

initial_prompt: |
  AI 이미지 기반 패션 무드 분석 & 크로스플랫폼 상품 추천 서비스 (POC)
  src/app/ (App Router), src/components/ (shadcn/ui), src/lib/ (유틸)
  Tech: Next.js 16, React 19, Tailwind 4, shadcn/ui, framer-motion, GPT-4o-mini Vision

symbol_info_budget:
language_backend:
